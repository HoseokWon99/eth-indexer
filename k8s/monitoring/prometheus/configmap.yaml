apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: eth-indexer
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s

    scrape_configs:
      - job_name: kafka
        static_configs:
          - targets: ['kafka-exporter:9308']
