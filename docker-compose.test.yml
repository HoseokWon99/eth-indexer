services:
  # Anvil - Local Ethereum node
  anvil:
    build:
      context: .
      dockerfile: test/anvil/Dockerfile
    container_name: eth-indexer-anvil
    environment:
      FORK_URL: ${FORK_URL}
    ports:
      - "8545:8545"
    networks:
      - eth-indexer-test
    healthcheck:
      test: ["CMD", "cast", "client", "--rpc-url", "http://localhost:8545"]
      interval: 2s
      timeout: 5s
      retries: 10

  # Postgres - Test database
  postgres:
    image: postgres:16-alpine
    container_name: eth-indexer-postgres-test
    environment:
      POSTGRES_DB: eth_indexer_test
      POSTGRES_USER: indexer
      POSTGRES_PASSWORD: indexer_password
    ports:
      - "5434:5432"
    networks:
      - eth-indexer-test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U indexer -d eth_indexer_test"]
      interval: 5s
      timeout: 5s
      retries: 5
    # No volumes - fresh state on each run

  # Valkey - Test cache
  valkey:
    image: valkey/valkey:7-alpine
    container_name: eth-indexer-valkey-test
    ports:
      - "6380:6379"
    networks:
      - eth-indexer-test
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    # No persistence - fresh state on each run

  # Contract Deployer - One-shot service
  contract-deployer:
    image: ghcr.io/foundry-rs/foundry:latest
    container_name: eth-indexer-contract-deployer
    depends_on:
      anvil:
        condition: service_healthy
    environment:
      RPC_URL: http://anvil:8545
    volumes:
      - ./test/contracts:/workspace
      - ./.scripts/anvil:/.scripts
      - ./deployed-addresses.json:/workspace/deployed-addresses.json
    working_dir: /workspace
    command: /.scripts/deploy-contracts.sh
    networks:
      - eth-indexer-test
    restart: "no"

  # Indexer - Service under test
  indexer:
    build:
      context: .
      dockerfile: services/indexer/Dockerfile
    container_name: eth-indexer-test
    depends_on:
      postgres:
        condition: service_healthy
      contract-deployer:
        condition: service_completed_successfully
    environment:
      INDEXER_CONFIG_PATH: /etc/eth-indexer/indexer-config.json
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: eth_indexer_test
      POSTGRES_USER: indexer
      POSTGRES_PASSWORD: indexer_password
    volumes:
      - ./config/config.test.json:/etc/eth-indexer/indexer-config.json:ro
      - ./test-state:/var/lib/eth-indexer/test-state
    networks:
      - eth-indexer-test

  # API Server - Service under test
  api-server:
    build:
      context: .
      dockerfile: services/api-server/Dockerfile
    container_name: eth-api-server-test
    depends_on:
      postgres:
        condition: service_healthy
      valkey:
        condition: service_healthy
    environment:
      API_PORT: 8081
      API_TTL: 300
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_DB: eth_indexer_test
      POSTGRES_USER: indexer
      POSTGRES_PASSWORD: indexer_password
      REDIS_HOST: valkey
      REDIS_PORT: 6379
      TOPICS: Transfer,Approval
    ports:
      - "8081:8081"
    networks:
      - eth-indexer-test
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/health"]
      interval: 5s
      timeout: 3s
      retries: 10

networks:
  eth-indexer-test:
    name: eth-indexer-test
    driver: bridge
